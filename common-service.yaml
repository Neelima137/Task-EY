apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  selector:
    app: nginx
    version: blue     # initially routes to blue
  ports:
    - port: 80
      targetPort: 80
  type: LoadBalancer